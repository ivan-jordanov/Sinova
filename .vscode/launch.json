{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI: Run & Debug",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}/sinova-backend",
            "args": [
                "app.main:app",
                "--host",
                "127.0.0.1",
                "--reload",
                "--port",
                "8000"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}/sinova-backend"
            },
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
