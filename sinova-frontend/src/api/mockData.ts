import type { DatasetMetadata } from "../types/dataset";

export const mockDataset: DatasetMetadata = {
  name: "spider.mraw",
  detectorWidth: 2048,
  detectorHeight: 2048,
  projections: 1800,
  slices: 2048,
  format: "MRAW",
};
